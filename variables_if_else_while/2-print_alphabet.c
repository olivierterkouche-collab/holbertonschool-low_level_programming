#include <stdio.h>

/**
 * main - A for loop that prints the alphabet in lower case
 * Return: Returns 0 if the code worked as intended
 */
int main(void)
{
	char i;

	for (i = 97; i <= 122; i++)
	{
	putchar(i);
	}
	i = 10;
	putchar(i);
	return (0);
}
