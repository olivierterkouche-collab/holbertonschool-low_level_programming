#include <stdio.h>

/**
 * main - prints a string of characters unsing printf and not puts this time
 * Return: returns the value 0 when the text has been sucessfully printed
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
