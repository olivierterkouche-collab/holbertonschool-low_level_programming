#include <stdio.h>

/**
 * main - Prints a string of characters using puts and not printf
 * Return: 0 to exit program after the text printing
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle\n");
	return (0);
}
